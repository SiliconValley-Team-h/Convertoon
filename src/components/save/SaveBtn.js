function SaveBtn() {
  return (
    <button
      className="SaveBtn"
      style={{
        color: 'black',
        backgroundColor: '#fcdd9e',
        border: 0,
        width: '70px',
        borderRadius: '40px',
        textAlign: 'center',
        position: 'absolute',
        right: '20px',
      }}
    >
      저장
    </button>
  );
}

export default SaveBtn;
